<p>This assessment is set up to allow you to try multiple variants of
the same question. Each of these variants is equivalent to the
question you have been presented with, but may include changes in
input values, parameters and other settings. Although
<% if (variantAttemptsLeft > 1) { %>
you have <%= variantAttemptsLeft %> attempts left
<% } else if (variantAttemptsTotal > 1) { %>
this is your last attempt
<% } else { %>
you have a single attempt
<% } %>
with the current variant, you are allowed to try unlimited other
variants.</p>
