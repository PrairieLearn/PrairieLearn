columns
    id: bigint not null default nextval('topics_id_seq'::regclass)
    name: text
    number: integer
    color: text
    description: text
    course_id: bigint not null

indexes
    topics_pkey: PRIMARY KEY (id)
    topics_course_id_name_key: UNIQUE (course_id, name)

referenced by
    questions: FOREIGN KEY (topic_id) REFERENCES topics(id) ON UPDATE CASCADE ON DELETE SET NULL