columns
    id: bigint not null default nextval('cron_jobs_id_seq'::regclass)
    name: text not null unqiue
    date: timestamptz not null

indexes
    cron_jobs_pkey: PRIMARY KEY (id)
    cron_jobs_name_key: UNIQUE (name)
