columns
    fetched_at: timestamp with time zone not null default now()
    guid: text not null
    id: bigint not null default nextval('cached_blog_posts_id_seq'::regclass)
    link: text not null
    pub_date: timestamp with time zone not null
    title: text not null

indexes
    cached_blog_posts_pkey: PRIMARY KEY (id) USING btree (id)
    cached_blog_posts_guid_key: UNIQUE (guid) USING btree (guid)
    cached_blog_posts_pub_date_idx: USING btree (pub_date DESC)
