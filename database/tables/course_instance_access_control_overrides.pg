columns
    course_instance_id: bigint not null
    enabled: boolean not null default false
    id: bigint not null default nextval('course_instance_access_control_overrides_id_seq'::regclass)
    name: text
    published_end_date: timestamp with time zone

indexes
    course_instance_access_control_overrides_pkey: PRIMARY KEY (id) USING btree (id)
    course_instance_access_control_overrides_course_instance_id_idx: USING btree (course_instance_id)
    course_instance_access_control_overrides_course_instance_id_name_unique: UNIQUE (course_instance_id, name)

foreign-key constraints
    course_instance_access_control_override_course_instance_id_fkey: FOREIGN KEY (course_instance_id) REFERENCES course_instances(id) ON UPDATE CASCADE ON DELETE CASCADE

referenced by
    course_instance_access_control_enrollment_overrides: FOREIGN KEY (course_instance_access_control_override_id) REFERENCES course_instance_access_control_overrides(id) ON UPDATE CASCADE ON DELETE CASCADE
