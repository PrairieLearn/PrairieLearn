import base64

import prairielearn as pl


def parse(data):
    contents = base64.b64encode(b"Submitted data.").decode()
    pl.add_submitted_file(data, "submission.txt", contents)


def file(data):
    if data["filename"] == "file.txt":
        return "This data is generated by code."
