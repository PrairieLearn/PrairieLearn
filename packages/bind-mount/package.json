{
  "name": "@prairielearn/bind-mount",
  "version": "1.0.0",
  "dependencies": {
    "bindings": "^1.5.0",
    "nan": "^2.16.0"
  },
  "scripts": {
    "build": "rm -rf build/ && node-gyp configure && node-gyp build"
  }
}
