---
'@prairielearn/eslint-plugin': patch
---

Use the `@typescript/typescript6` compatibility package for compiler API access, keeping the TypeScript 6 API available alongside the TypeScript 7 build compiler.
