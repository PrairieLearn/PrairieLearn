---
'@prairielearn/postgres': minor
'@prairielearn/migrations': minor
---

Export `escapeIdentifier` for safely constructing SQL queries with dynamic identifiers, and add a reusable batched migration helper for selecting table ID bounds.
