name: 'Label PRs'
on:
  - pull_request_target

jobs:
  label-by-changes:
    name: 'PR changes'
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v6
        with:
          sync-labels: false
  # labeler cannot label PRs by title: https://github.com/actions/labeler/issues/55
  label-by-title:
    # To prevent labeler from removing labels added by this job, it should run beforehand.
    needs: [label-by-changes]
    name: 'PR title'
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: fuxingloh/multi-labeler@5bc612d4ba82908c019275f06ba05fc33e92650f # v4
        with:
          config-path: .github/title-labeler.yml
