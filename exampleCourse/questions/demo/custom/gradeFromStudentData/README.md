This question is graded using data provided by the student, as in a lab experiment. The recorded measurements are sanity-checked in the `parse()` function and graded against reference values with generous tolerances. The `grade()` function in `server.py` then computes the expected value of `g` from the student's own measurements, so any answer consistent with their recorded data receives full credit.
