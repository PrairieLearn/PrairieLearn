This question is graded using data provided by the student, as in a lab experiment. The recorded measurements have no correct answer and are not graded directly; they are only sanity-checked in the `parse()` function. The `grade()` function in `server.py` computes the expected value of `g` from the student's own measurements, so any answer consistent with their recorded data receives full credit.
