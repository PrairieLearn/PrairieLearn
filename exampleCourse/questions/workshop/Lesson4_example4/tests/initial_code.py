import numpy as np


def array_to_scalar(a, b):
    """
    a,b: 2d numpy arrays of the same shape
    This function performs element-wise multiplication of a and b,
    and returns a tuple (c,sumc), where c is the array resulting from
    the multiplication, and sumc is the sum of all entries of c.
    """
    pass
