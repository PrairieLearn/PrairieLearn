services:
  pl:
    build:
      context: .
    image: prairielearn/prairielearn:local
    ports:
      - 3000:3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOME}/pl_ag_jobs:/jobs
      - .:/PrairieLearn
      # To test local changes to workspaces or external graders
      # or override other defaults from ConfigSchema, 
      # enable the following line and make your edits.
      # - config.dev.json:/PrairieLearn/config.json
    container_name: pl
    environment:
      - HOST_JOBS_DIR=${HOME}/pl_ag_jobs
      - DEV=true
    # This must be changed if you've changed Docker's address pools.
    # i.e., "default-address-pools" in /etc/docker/daemon.json
    extra_hosts:
      - 'host.docker.internal:172.17.0.1'
