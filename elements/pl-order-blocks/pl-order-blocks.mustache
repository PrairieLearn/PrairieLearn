{{#question}}
    <div class="row">
        <div class="column {{dropzone_layout}}">
            <ul id="{{answer_name}}-options" name="{{answer_name}}" class="pl-order-blocks-connected-sortable">
                <li class='ui-sortable-handle info info-fixed'>{{source-header}}</li>
                    {{#options}}
                        <li string="{{.}}" data-title="{{.}}">{{.}}</li>
                    {{/options}}
                </ul>
        </div>
            <div class="column {{dropzone_layout}}">
                <ul id="{{answer_name}}-dropzone" name="{{answer_name}}" class="dropzone pl-order-blocks-connected-sortable {{check_indentation}}">
                    <li class="ui-sortable-handle info info-fixed">{{solution-header}}</li>
                    {{#submission_dict}}
                        <li string="{{text}}" data-title="{{text}}" style='margin-left: {{indent}}px;'>{{text}}</li>
                    {{/submission_dict}}
                </ul>
            </div>
        </div>
    <input id="{{answer_name}}-input" type="hidden" name="{{answer_name}}-input" value=""/>

{{/question}}

{{#submission}}
    {{^parse-error}}
        <div style="margin-bottom: 5px;"><strong>Your answer: </strong>
        {{#student_submission}}
        		<p class="answer_block"> {{text}} </p>
        {{/student_submission}}
        {{#perfect_score}}
            <span class="badge {{color}}"><i class="fa fa-check" aria-hidden="true"></i> 100%</span>
        {{/perfect_score}}
        {{^perfect_score}}
                <span class="badge {{color}}"><i class="fa fa-times" aria-hidden="true"></i>
        {{score}}%</span> {{feedback}}
        {{/perfect_score}}
        </div>
    {{/parse-error}}

    {{#parse-error}}
        <div style="margin-bottom: 5px;"><strong>Your answer: </strong></div>
        <div id="pl-order-blocks-{{uuid}}" class="d-inline-block">
            <span class="badge text-danger badge-invalid"><i class="fa fa-exclamation-triangle" aria-hidden="true"></i> Invalid</span>
            <a role="button" class="btn btn-sm btn-secondary small border" data-placement="auto" data-trigger="focus" data-toggle="popover" data-html="true" style="margin-left: 5px" title="Format Error" tabindex="0" data-content="{{parse-error}}"> Why <i class="fa fa-question-circle" aria-hidden="true"></i></a>
        </div>
    {{/parse-error}}
    <div>{{data}}</div>
{{/submission}}

{{#true_answer}}
    <div style="margin-bottom: 7px;"><strong>Correct answer ({{grading_mode}}{{check_indentation}}):</strong>
    {{#question_solution}}
        <p class="answer_block"> {{text}} </p>
    {{/question_solution}}
    </div>
{{/true_answer}}
