id: fix-index-files
language: TypeScript
rule:
  any:
    - pattern: import $IMPORT from '$LOCATION';
constraints:
  LOCATION:
    regex: '\./(cron|sprocs|news_items)$'
fix: import $IMPORT from '$LOCATION/index';
