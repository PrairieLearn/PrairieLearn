# `prairielearn/grader-python`

View documentation at <https://docs.prairielearn.com/python-grader>.

The Python autograding docker image is built off of the amazonlinux 2023 docker image.
Python 3.13 is installed on the image alongside an assortment of Python packages
found in the `requirements.txt` file.
