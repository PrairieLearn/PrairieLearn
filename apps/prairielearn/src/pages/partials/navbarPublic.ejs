<li class="nav-item btn-group">
  <a class="nav-link" aria-label="Link to page showing all public questions for the course." href="<%= urlPrefix %>/public/course/<%= course.id %>/questions">
    <% if (locals.course) { %>
      <%= course.short_name %>
    <% } %>
  </a>
</li>
