// The implicit module that sync always creates and that every assessment
// without an explicit `module` falls back to. It can't be deleted or renamed
// from the editor, which is what guarantees it always exists.
export const DEFAULT_ASSESSMENT_MODULE_NAME = 'Default';
