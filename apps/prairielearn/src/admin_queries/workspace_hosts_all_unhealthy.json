{
    "description": "Set all workspace hosts to unhealthy.",
    "params": [
        {
            "name": "unhealthy_reason",
            "description": "Reason for setting all workspace hosts to unhealthy.",
            "default": "admin query"
        }
    ]
}
