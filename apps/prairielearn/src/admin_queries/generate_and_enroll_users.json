{
  "description": "Generate random users and enroll them in a course instance",
  "params": [
    {
      "name": "count",
      "description": "Number of users to generate (integer)",
      "default": "30"
    },
    {
      "name": "course_instance_id",
      "description": "course_instance_id to enroll the users in (integer), can be blank to only create the users and not enroll them"
    }
  ]
}
