{
  "description": "Simulates all students enrolled in a course starting an assessment (groupwork not supported)",
  "params": [
    {
      "name": "assessment_id",
      "description": "assessment_id to generate instances for (integer)"
    },
    {
      "name": "mode",
      "description": "mode used for assessment instance (Public or Exam)",
      "default": "Public"
    }
  ]
}
