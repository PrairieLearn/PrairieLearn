{
  "description": "Simulates all students enrolled in a course starting an assessment. For group assessments, groups must have been created.",
  "params": [
    {
      "name": "assessment_id",
      "description": "assessment_id to generate instances for (integer)"
    },
    {
      "name": "mode",
      "description": "mode used for assessment instance (Public or Exam)",
      "default": "Public"
    }
  ]
}
