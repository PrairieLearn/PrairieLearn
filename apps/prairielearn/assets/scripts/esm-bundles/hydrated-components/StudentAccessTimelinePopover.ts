import { registerHydratedComponent } from '@prairielearn/react/hydrated-component';

import { StudentAccessTimelinePopover } from '../../../../src/components/StudentAccessRulesPopover.js';

registerHydratedComponent(StudentAccessTimelinePopover);
