const { html } = require('@prairielearn/html');
const { renderEjs } = require('@prairielearn/html-ejs');

const SamlTest = ({ resLocals }) => {
  return html``.toString();
};

module.exports.SamlTest = SamlTest;
