{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "public/build/**"]
    },
    "build-slim": {
      "dependsOn": ["^build-slim"],
      "outputs": ["dist/**", "public/build/**"]
    }
  }
}
